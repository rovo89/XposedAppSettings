<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.robv.android.xposed.mods.appsettings"
    android:versionCode="21"
    android:versionName="1.5" xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk android:minSdkVersion="15" tools:ignore="UsesMinSdkAttributes"/>
    <permission-tree android:name="de.robv.android.xposed.mods.appsettings"/>
    <uses-permission android:name="de.robv.android.xposed.mods.appsettings.BROADCAST_PERMISSION"/>
    <permission android:name="de.robv.android.xposed.mods.appsettings.BROADCAST_PERMISSION" android:protectionLevel="dangerous"></permission>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".XposedModActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
           	android:name=".settings.ApplicationSettings"
            android:label="@string/settings_title"
            android:windowSoftInputMode="stateHidden"
            android:configChanges="orientation|screenSize">
        </activity>
        
        <receiver android:name=".lockscreen_launcher.LockscreenLauncherProvider" >
		    <intent-filter>
		        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
		        <action android:name="de.robv.android.xposed.mods.appsettings.lockscreen_launcher.CLICK_ACTION" />
		    </intent-filter>
		    <meta-data android:name="android.appwidget.provider"
		               android:resource="@xml/lockscreen_launcher_provider" />
		</receiver>
		<!-- The service serving the RemoteViews to the launcher widget -->
        <service android:name=".lockscreen_launcher.LauncherWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/app_mod_summary" />
        <meta-data
            android:name="xposedminversion"
            android:value="30+" />
    </application>

</manifest>
